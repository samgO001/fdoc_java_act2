package com.example;

import java.util.Locale;
import java.util.Scanner;

public class Main {
    public static void main(String[] args) {
        // Programa con varios ejercicios sobre ciclos.
        // Todos los ejercicios están implementados dentro de este método `main`.
        Locale.setDefault(Locale.US);
        Scanner sc = new Scanner(System.in);

        // Datos de ejemplo reutilizables
        int[] sampleArray = {3, 7, 1, 9, 4, 6, 2};
        String[] words = {"uno", "dos", "tres", "cuatro"};

        // Ejercicios se añadirán en commits posteriores por grupos (While, Do-while, For, For-each, Break/Continue).

        System.out.println("Ejercicios de ciclos - arranque del programa (implementado en main).");

        // Nota: se proveen mensajes para guiar la ejecución cuando se realicen pruebas manuales.

        sc.close();
    }
}